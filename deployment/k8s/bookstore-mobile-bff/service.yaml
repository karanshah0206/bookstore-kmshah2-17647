apiVersion: v1
kind: Service
metadata:
  name: bookstore-mobile-bff
  namespace: bookstore-ns
spec:
  selector:
    app: bookstore-mobile-bff
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: LoadBalancer
