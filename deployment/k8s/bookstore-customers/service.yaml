apiVersion: v1
kind: Service
metadata:
  name: bookstore-customers
  namespace: bookstore-ns
spec:
  selector:
    app: bookstore-customers
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
